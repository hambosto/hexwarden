package main

import "fmt"

func main() {
	// key := make([]byte, 32)
	// rand.Read(key)

	// c, _ := cipher.New(key)

	// enc, _ := c.Encrypt([]byte("data"))

	// dec, _ := c.Decrypt(enc)

	// fmt.Println(string(enc))
	// fmt.Println(string(dec))

	a := 10
	b := 10

	count := (a + b)

	fmt.Println(count)
}
